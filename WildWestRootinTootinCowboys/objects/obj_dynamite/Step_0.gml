/// @description Insert description here
// You can write your code in this editor

depth = -y - 50;

hspeed += -sign(hspeed) / 10;
vspeed += -sign(vspeed) / 10;

image_angle += abs(hspeed) + abs(vspeed);